{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "jquery",
      "waypoints"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
